var text = 'Carlos';

var n = 1+1; //?



var data ="ddfsd".reduce((prev, char)=>{
    if(char !== 'l'){
        return prev.concat(char);
    }
    else
        return prev;
}, "");


   var data = text.reduce((prev, char)=>{
    if(char !== 'l'){
        return prev.concat(char);
    }
    else
        return prev;
}, "");


data //?
